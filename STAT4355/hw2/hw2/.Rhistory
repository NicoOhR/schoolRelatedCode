remotes::install_github("tallguyjenks/gruvboxr")
data = read.csv("./hw2_systolic_bp(1).csv")
View(data)
model <- lm(weight ~ sys.bp, data = data)
View(model)
summary(model)
#Want to test H_0 : \beta_1 = -50, first we calculate the test statistic
beta_1_hat <- coef(model)[2]
m <- summary(model)
Sxx <- sum((data$weight - mean(data$weight)))^2
Sxx <- sum((data$weight - mean(data$weight))^2)
test_statistic <- (beta_1_hat + 50)/sqrt((m$sigma^2)/(Sxxl))
test_statistic <- (beta_1_hat + 50)/sqrt((m$sigma^2)/(Sxx))
#then we can calculate value of the t distribution
n <- nrows(data$weight)
#then we can calculate value of the t distribution
n <- nrow(data$weight)
#then we can calculate value of the t distribution
n <- nrow(data$weight)
#then we can calculate value of the t distribution
n <- nrow(data)
qt(0.95, n -2)
t_distribution <- qt(0.95, n -2)
#Want to test H_0 : \beta_1 = -50, first we calculate the test statistic
beta_1_hat <- coef(model)[2]
Sxx <- sum((data$weight - mean(data$weight))^2)A
#Want to test H_0 : \beta_1 = -50, first we calculate the test statistic
beta_1_hat <- coef(model)[2]
Sxx <- sum((data$weight - mean(data$weight))^2)
sigma_hat <- m$sigma
test_statistic <- (beta_1_hat - (-50))/sqrt(sigma_hat^2/Sxx)
#then we can calculate value of the t distribution
n <- nrow(data)
t_distribution <- qt(0.975, n -2)
#Want to test H_0 : \beta_1 = -50, first we calculate the test statistic
beta_1_hat <- coef(model)[2]
Sxx <- sum((data$weight - mean(data$weight))^2)
sigma_hat <- m$sigma
test_statistic <- (beta_1_hat - (-50))/sqrt(sigma_hat^2/Sxx)
#then we can calculate value of the t distribution
n <- nrow(data)
t_critical <- qt(0.975, n -2)
m <- summary(model)
m
#Want to test H_0 : \beta_1 = -50, first we calculate the test statistic
beta_1_hat <- coef(model)[2]
Sxx <- sum((data$weight - mean(data$weight))^2)
sigma_hat <- m$sigma
test_statistic <- (beta_1_hat - (1.4265))/sqrt(sigma_hat^2/Sxx)
#then we can calculate value of the t distribution
n <- nrow(data)
t_critical <- qt(0.975, n -2)
beta_1_hat <- coef(model)[2]
Sxx <- sum((data$weight - mean(data$weight))^2)
sigma_hat <- m$sigma
test_statistic <- (beta_1_hat - (-50))/sqrt(sigma_hat^2/Sxx)
