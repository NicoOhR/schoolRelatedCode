#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <vector>


using namespace std;
/*

main:
  iterate over csv:
    * for every point, calculate the voxel it would be in
    * append the center coordinate of the voxel to output.csv
  iterate over output.csv:
    * removing entries which match prior entry

*/



int main(){

  ifstream inputFile;
  inputFile.open("input.csv");
  
  return(0);
}
