{"cmd": "def grad_decent(grad_loss, dataset, epochs):\n    eta = 0.01\n    data = np.asarray(dataset[\"data\"])\n    target = np.asarray(dataset[\"target\"])\n    w = np.zeros(data.shape[1], dtype=float)\n    b = 0.0\n    for _ in tqdm(epochs):\n        grad_w = np.zeros(data.shape[1], dtype=float)\n        grad_b = 0.0\n        for x, y in zip(data, target):\n            g_w, g_b = grad_loss(w, b, x, y)\n            grad_w += g_w\n            grad_b += g_b\n        w -= grad_w * eta\n        b -= grad_b * eta\n    return w, b\n\n\ndef main():\n    dataset = boston_housing.load_dataset()\n    w, b = grad_decent(grad_mse, dataset, epochs=200)\n    # print(\"final_mse:\", history[-1])\n    print(\"w_shape:\", w.shape)\n    print(\"b:\", b)\n\n\nif __name__ == \"__main__\":\n    main()", "cmd_opts": "", "import_complete": 1, "terminal": "nvimterm"}